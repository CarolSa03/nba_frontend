# NBA Ties Viewer

A small project I did for someone special.

## Quick Start

Use `start.bat` for simplicity. It will automatically:
- install dependencies
- create `.env` file
- start the development server

## Requirements

- node.js 16+
- npm
